import { SideBar } from '../components/SideBar';

export const MessagePage: React.FC = () => {
  return (
    <SideBar>
      <h2 className="mt-4 text-center">Message Page</h2>
    </SideBar>
  );
};
