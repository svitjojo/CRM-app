import { SideBar } from '../components/SideBar';

export const CalendarPage: React.FC = () => {
  return (
    <SideBar>
      <h2 className="mt-4 text-center">Calendar Page</h2>
    </SideBar>
  );
};
